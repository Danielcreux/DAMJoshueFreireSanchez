#include <iostream>                       //Incluyo una librería de flujo(stream de entrada (input) y salida (output)

int main(){                               //Creo una función,pero es el punto de entrada de la ejecución, la función
    int edad=46.00;
    std::string nombre="Joshue Daniel";    //de la libreria std importo el dato string 
    std::cout <<"hola mundo desde C++ y me llamo"<<nombre<<"Y estoy muy contento de aprender c++";  //De la libreia estanda(std) quiero usar el comando C-out, y saco una impresion  
    return 0;                           //Cumplo la promesa de devolver un entero
}