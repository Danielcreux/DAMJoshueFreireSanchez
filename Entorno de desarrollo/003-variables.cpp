#include <iostream>                       //Incluyo una librería de flujo(stream de entrada (input) y salida (output)

int main(){                               //Creo una función,pero es el punto de entrada de la ejecución, la función
    int edad=46;
    std::cout <<"hola mundo desde C++";  //De la libreia estanda(std) quiero usar el comando C-out, y saco una impresion  
    return 0;                           //Cumplo la promesa de devolver un entero
}